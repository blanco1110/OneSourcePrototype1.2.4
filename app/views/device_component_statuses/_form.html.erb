<%= form_with(model: device_component_status, local: true) do |form| %>
  <% if device_component_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(device_component_status.errors.count, "error") %> prohibited this device_component_status from being saved:</h2>

      <ul>
      <% device_component_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :device_comp_status %>
    <%= form.text_field :device_comp_status, id: :device_component_status_device_comp_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
